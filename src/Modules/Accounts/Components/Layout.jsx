const Layout = ({

}) => {
    return <div class="mx-auto max-w-lg h-full grid place-items-center">
        <input
            class="border"
            value={phoneNumber.value}
        />
    </div>
}

export default Layout
